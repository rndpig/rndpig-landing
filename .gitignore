# Dependencies
node_modules/

# Secrets / environment
.env
.env.*
!.env.example
*-service-account*.json
*serviceAccount*.json
*.pem

# Tunnel-ingress rollback snapshots — these hold the internal
# hostname -> localhost:PORT map for every app on dilger, and this
# repo is served publicly at rndpig.com. Keep them local.
scripts/provision/backups/

# Build output
dist/
build/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code agent worktrees
.claude/worktrees/
